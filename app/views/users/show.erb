<div class="container">
    <div class="row">
      <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
        <div class="card card-signin my-5">
          <div class="card-body">
<div class="container">
</br>
<h3><%= current_user.name %></h3>
<dl>
<dd>Username: <%=current_user.username%></dd>
<dd>Email: <%=current_user.email%></dd>
</dl>
</br>
<h4>Posts created:</h4>
<% @fintechs.each do |fintech|%>
<% if current_user.id == fintech.user_id %>
<div class="container">
<a href="/fintechs/<%=fintech.id%>"><%=fintech.name%></a>
</div>
<%end%>
<%end%>
<h4>Reviews created:</h4>
<%@reviews.each do |review|%>
<% if current_user.id == review.user_id %>
<div class="container">
<a href='/reviews/<%=review.id%>'><%=review.company_name%> - <%=review.created_at%></a>
</div>
<%end%>
<%end%>
</br>
<button type="submit" class="btn btn-primary"><a class="text-white" href="/<%=current_user.id%>/edit">Edit profile</a></button>
</br>
</br>
 </div>
</div>
        </div>
      </div>
    </div>
  </div>
</form>
